{
	"name": "nuxt-app",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "nuxt build",
		"dev": "nuxt dev",
		"generate": "nuxt generate",
		"preview": "nuxt preview",
		"postinstall": "nuxt prepare",
		"postgenerate": "node scripts/mergeIndex.js && mv ./dist/* ../web/",
		"pregenerate": "rm -rf ../web/_nuxt"
	},
	"dependencies": {
		"jsdom": "^25.0.1",
		"nuxt": "^3.13.0",
		"vue": "latest",
		"vue-router": "latest"
	}
}
